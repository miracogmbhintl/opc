---
const workspaceId = String(Astro.params.workspaceId || '').trim();
const target = workspaceId
  ? `/work-os/boards?workspace_id=${encodeURIComponent(workspaceId)}`
  : '/work-os/boards';

return Astro.redirect(target, 302);
---
