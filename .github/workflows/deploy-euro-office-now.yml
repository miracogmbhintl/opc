name: Deploy Office Now

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/deploy-euro-office-now.yml'
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-euro-office-container.yml
