services:
  documentserver:
    image: ghcr.io/euro-office/documentserver:latest
    container_name: opc-euro-office
    restart: unless-stopped
    ports:
      - "127.0.0.1:${EURO_OFFICE_PORT:-8080}:80"
    environment:
      EXAMPLE_ENABLED: "false"
      JWT_SECRET: "${EURO_OFFICE_JWT_SECRET}"
